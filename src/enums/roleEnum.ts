/**
 * 角色定义，通常前端控制权限用得到
 *
 * Role definition, usually used for front-end control permission
 */
export enum RoleEnum {
  /**
   * 超管
   *
   * Admin
   */
  ADMIN = 'admin',

  /**
   * 测试
   *
   * Test
   */
  TEST = 'test',
}
